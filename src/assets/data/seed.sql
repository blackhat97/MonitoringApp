CREATE TABLE IF NOT EXISTS settings(sensorId INTEGER NOT NULL, userId INTEGER NOT NULL, tempSign BOOLEAN DEFAULT 1, tempUnit TEXT DEFAULT 'C', storeInt INTEGER DEFAULT 5, storeTerm INTEGER DEFAULT 365);
